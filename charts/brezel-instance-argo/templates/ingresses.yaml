apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: brezel-api
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    cert-manager.io/cluster-issuer: {{ .Values.cluster_issuer }}
    nginx.ingress.kubernetes.io/proxy-body-size: "2048m"
spec:
  tls:
  {{- if .Values.secure }}
  - hosts: {{ toJson .Values.api_hostnames }}
    secretName: brezel-tls
  {{- end }}
  rules:
  {{- range .Values.api_hostnames }}
  - host: {{ . }}
    http:
      paths:
      - backend:
          service:
            name: brezel
            port:
              number: 80
        path: /
        pathType: Prefix
  {{- end }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: brezel-spa
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    cert-manager.io/cluster-issuer: {{ .Values.cluster_issuer }}
spec:
  tls:
  {{- if .Values.secure }}
  - hosts: {{ toJson .Values.hostnames }}
    secretName: brezel-spa-tls
  {{- end }}
  rules:
  {{- range .Values.hostnames }}
  - host: {{ . }}
    http:
      paths:
      - backend:
          service:
            name: brezel-spa
            port:
              number: 80
        path: /
        pathType: Prefix
  {{- end }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: phpmyadmin
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    cert-manager.io/cluster-issuer: {{ .Values.cluster_issuer }}
    nginx.ingress.kubernetes.io/proxy-body-size: "2048m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
spec:
  tls:
  {{- if .Values.secure }}
  - hosts:
    - {{ .Values.pma_hostname }}
    secretName: phpmyadmin-tls
  {{- end }}
  rules:
  - host: {{ .Values.pma_hostname }}
    http:
      paths:
      - backend:
          service:
            name: phpmyadmin
            port:
              number: 80
        path: /
        pathType: Prefix
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: brotcast
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    cert-manager.io/cluster-issuer: {{ .Values.cluster_issuer }}
spec:
  tls:
  {{- if .Values.secure }}
  - hosts:
    - {{ .Values.brotcast_hostname }}
    secretName: brotcast-tls
  {{- end }}
  rules:
  - host: {{ .Values.brotcast_hostname }}
    http:
      paths:
      - backend:
          service:
            name: brotcast
            port:
              number: 8086
        path: /
        pathType: Prefix

